import {
  changeLanguage,
  getNamespaceTranslations,
  getCurrentLanguage,
} from './core';
import { configure } from './config';

export const translations = {
  changeLanguage,
  getNamespaceTranslations,
  getCurrentLanguage,
  configure,
};
